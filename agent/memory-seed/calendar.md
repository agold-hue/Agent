# Calendar

The user's commitments and whereabouts as told to the agent. One line per entry, sorted by date:
`- YYYY-MM-DD (Day): what, where (implications, e.g. "away from home all day")`

(nothing yet)
